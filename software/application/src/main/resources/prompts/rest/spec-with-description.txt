You are an expert API mock generator. Generate WireMock JSON mappings based on this API specification and enhancement description:

API Specification Summary:
- Title: {{SPEC_TITLE}}
- Version: {{SPEC_VERSION}}
- Endpoints: {{ENDPOINT_COUNT}}
- Key endpoints: {{KEY_ENDPOINTS}}

Namespace:
- API Name: {{API_NAME}}{{CLIENT_SECTION}}

Enhancement Description: {{DESCRIPTION}}

Requirements:

WHICH ENDPOINTS TO GENERATE (CRITICAL — read this before generating):
- Generate mocks ONLY for endpoints explicitly mentioned or implied by the Enhancement Description.
- If the description says "all GET endpoints", generate ONLY GET endpoints — do not include POST, PUT, DELETE, or other methods.
- If the description mentions specific endpoints (e.g., "error responses for pet endpoints"), generate mocks ONLY for those endpoints — not success responses for every endpoint in the spec.
- If the description does not restrict which endpoints to generate, generate mocks for ALL endpoints in the specification.
- Count your mappings carefully. Generating too many mappings risks exceeding the output token limit and producing truncated, unparseable JSON.
- One high-quality mapping per endpoint/scenario. Prefer fewer, correct mappings over many incomplete ones.

- Generate WireMock mappings that follow the API specification structure
- IMPORTANT: All mock URLs must be prefixed with /{{NAMESPACE}} (e.g., if the spec has /users, the mock URL should be /{{NAMESPACE}}/users)
- CRITICAL: Every mapping MUST include `"persistent": true` to ensure mocks survive Lambda cold starts and are stored in S3
- Enhance the mappings based on the description (add error cases, specific data, behaviors, etc.)
- Include realistic response data that matches both the spec and description
- Handle appropriate HTTP status codes
- Include relevant headers and proper content types
- Ensure data consistency and correct response format (IMPORTANT):
  - If an endpoint filters by query parameters (e.g. `status=available`), the response MUST only contain items with that status.
  - If an endpoint uses path parameters (e.g. `/pet/1`), the response data MUST match that ID.
  - Ensure you use the correct response format (Object vs Array) as defined in the specification for each endpoint.
  - When the description defines specific entities with properties (e.g., a pet with a specific name, status, or image),
    those entities MUST appear consistently across ALL relevant endpoints — list endpoints, detail endpoints, and filtered endpoints.
    For example, if pet ID=1 is a bird with status "available", then GET /pet/1, GET /pets, and GET /pets?status=available must all return consistent data for that bird.
  - If entities have tags or other filterable properties, ensure that filter/search endpoints return exactly the correct subset of entities matching that filter.
- Generate comprehensive mappings that cover the described scenarios
- Prefer `jsonBody` over `body` for JSON responses to ensure easy readability and structure
- WireMock URL matching rules (IMPORTANT):
  - Do NOT use OpenAPI-style placeholders like `{petId}` in `url` or `urlPath`. WireMock treats them as literal text.
  - Use exactly ONE of these per mapping (never combine them):
    - `url` (exact full URL match)
    - `urlPath` (exact path match)
    - `urlPattern` (regex full URL match)
    - `urlPathPattern` (regex path match)
  - For endpoints with path parameters (e.g. `/pet/{petId}`):
    - PREFER using specific IDs in `urlPath` (e.g., `/{{NAMESPACE}}/pet/1`) to create realistic and deterministic mocks.
    - Only use `urlPathPattern` with a regex (e.g., `/{{NAMESPACE}}/pet/[^/]+`) if the description implies a generic behavior or wide matching.
  - Regardless of matching style, the response body MUST match the format (Object vs Array) defined in the specification for that specific endpoint.
  - If you return multiple mappings for the same endpoint, they MUST be non-overlapping and deterministic:
    - Either match specific IDs (`/{{NAMESPACE}}/pet/1`, `/{{NAMESPACE}}/pet/2`, …), OR
    - Add additional request constraints (queryParameters, headers, bodyPatterns), OR
    - Use `priority` to ensure deterministic selection.
  - Never output multiple mappings that would all match the same request unless they differ by `priority` and are intended as fallback.

- Return only a JSON array of WireMock mappings. 
- Each item MUST be a valid WireMock stub mapping object and MUST include these required top-level fields:
 {
 "request": { ... },
 "response": { ... },
 "persistent": true
 }
- Optional WireMock top-level fields are allowed when needed (e.g., "priority").
- "persistent": true MUST be placed at the top level of the mapping object
- NEVER place "persistent" inside "response" or "request"
- NEVER include unknown fields inside "response" (WireMock will reject them)
- Only use fields defined in the WireMock stub mapping schema below. Any field not in this schema will be rejected.

{{WIREMOCK_SCHEMA}}

- Do not include any explanatory text, only the JSON array.
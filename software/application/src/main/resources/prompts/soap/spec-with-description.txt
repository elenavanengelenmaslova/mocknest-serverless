You are an expert API mock generator. Generate WireMock JSON mappings based on this SOAP/WSDL API specification and enhancement description:

API Specification Summary:
- Title: {{SPEC_TITLE}}
- Version: {{SPEC_VERSION}}
- Operations: {{ENDPOINT_COUNT}}
- Key operations: {{KEY_ENDPOINTS}}

Namespace:
- API Name: {{API_NAME}}{{CLIENT_SECTION}}

Enhancement Description: {{DESCRIPTION}}

Requirements:
- Generate WireMock mappings that follow the SOAP/WSDL specification structure
- CRITICAL: Every mapping MUST include `"persistent": true` to ensure mocks survive Lambda cold starts and are stored in S3
- Enhance the mappings based on the description (add error cases, specific data, behaviors, etc.)
- Include realistic XML response data that matches both the schema and description
- CRITICAL: If the Enhancement Description above mentions "fault", "error", "failure", or "not found" scenarios, you MUST generate SOAP Fault responses with HTTP status 500 — NOT normal success responses with status 200. Every SOAP fault mapping MUST have `"status": 500` in the response. See the "SOAP 1.2 fault format" section below for the exact structure.

SOAP 1.2 request matching rules (IMPORTANT):
- ALL SOAP requests MUST use POST method — never GET, PUT, DELETE, or any other method
- Match on the `action` parameter inside the `Content-Type` header using the EXACT soapAction value from the Key operations list above
  - CRITICAL: Copy the soapAction URL VERBATIM from the Key operations list. Do NOT invent URLs like http://tempuri.org/ or http://namespace/Operation — use the exact URL shown.
  - Example: `"headers": { "Content-Type": { "contains": "action=\"http://example.com/service/OperationName\"" } }`
- Do NOT use a `SOAPAction` header — SOAP 1.2 uses the `action` parameter in `Content-Type` instead
- Use `urlPath` for the service endpoint path
- The `urlPath` MUST be constructed by prepending `/{{NAMESPACE}}` to the FULL path extracted from the WSDL's `<soap12:address location="..."/>` element
  - CRITICAL: Extract the COMPLETE path portion after the host from the WSDL address. Do NOT shorten, truncate, or modify the path in any way.
  - Example 1: If WSDL address is `http://example.com/calculator.asmx`, the path is `/calculator.asmx`, so use `"urlPath": "/{{NAMESPACE}}/calculator.asmx"`
  - Example 2: If WSDL address is `http://example.com/banking-service/v1`, the path is `/banking-service/v1`, so use `"urlPath": "/{{NAMESPACE}}/banking-service/v1"`
  - Example 3: If WSDL address is `http://example.com/inventory/ws`, the path is `/inventory/ws`, so use `"urlPath": "/{{NAMESPACE}}/inventory/ws"`
  - WRONG: Do NOT drop path segments. For `http://example.com/banking-service/v1`, using `/{{NAMESPACE}}/v1` is INCORRECT — you must include the full path `/banking-service/v1`.
- Each mapping MUST target a different operation via its action parameter
- Generate one primary success stub per SOAP operation unless the description explicitly requires additional scenarios (e.g., error cases, multiple test inputs)
- CRITICAL: Generate ONLY what the description asks for. If the description says "only for X and Y operations", generate stubs ONLY for X and Y — do NOT add error handling stubs, validation stubs, or stubs for other operations. Extra stubs will cause the scenario to fail.

WHICH OPERATIONS TO GENERATE (CRITICAL — read this before generating):
- Generate mocks ONLY for operations explicitly mentioned in the Enhancement Description.
- If the description mentions a specific operation (e.g., "multiply", "GetAccount"), generate ONLY that operation — do NOT generate other operations from the WSDL.
- If the Enhancement Description does not specify particular operations, generate mocks for ALL operations listed in Key operations above.
- One high-quality mapping per selected operation. No more (unless the description asks for error variants).
- Never generate multiple stubs for the same SOAP operation unless request matchers are mutually exclusive (e.g., different XPath body matches or different action values)
- Prefer matching by URL path + POST method + Content-Type action parameter. Add XPath body matchers only when needed to disambiguate between stubs sharing the same endpoint and action
- Unless otherwise specified:
    - Generate exactly one mapping per SOAP operation unless request matchers are truly different.
    - Do not use a wildcard-nested matcher like "matches": ".*" inside matchesXPath.
- If an XPath matcher uses namespace prefixes (e.g., `soap:Envelope`), you MUST include the `xPathNamespaces` field in the request matcher to define those prefixes
  - Example: `"xPathNamespaces": { "soap": "http://www.w3.org/2003/05/soap-envelope" }`
- IMPORTANT: In a bodyPatterns item, `xPathNamespaces` MUST be a sibling of `matchesXPath`, not nested inside it.
- Valid example:
  { "matchesXPath": "//tns:Multiply", "xPathNamespaces": { "tns": "http://tempuri.org/" } }
- Invalid example:
  { "matchesXPath": { "expression": "//tns:Multiply", "xPathNamespaces": { "tns": "http://tempuri.org/" } } }
- When possible, prefer `matchesXPath` as a simple string.
- If matching scalar values in XPath, use quoted text values like `text()='6'`, not `text()=6`.
- Preserve WSDL scalar types exactly.
- Do not emit duplicate mappings.

SOAP 1.2 response format rules (IMPORTANT):
- ALL SOAP responses MUST be valid XML SOAP 1.2 envelopes
- Use envelope namespace `http://www.w3.org/2003/05/soap-envelope`
- Response Content-Type MUST be `application/soap+xml; charset=utf-8`
- The target namespace for response elements MUST match the targetNamespace from the WSDL exactly — do NOT use http://tempuri.org/ unless the WSDL specifies it
- CRITICAL: The response element inside `<soapenv:Body>` MUST use a SEPARATE namespace prefix (e.g., `tns:`) bound to the WSDL targetNamespace — NOT the `soapenv:` prefix. The `soapenv:` prefix is ONLY for SOAP envelope elements (Envelope, Header, Body, Fault).
  - CORRECT: `<tns:GetAccountResponse xmlns:tns="http://example.com/banking-service">` — uses `tns:` prefix with the WSDL targetNamespace
  - WRONG: `<soapenv:GetAccountResponse>` — uses the envelope prefix, which binds to `http://www.w3.org/2003/05/soap-envelope` instead of the WSDL targetNamespace
- The WSDL targetNamespace is shown in the compact WSDL as `targetNamespace:`. Copy it EXACTLY into `xmlns:tns="..."`.
- Example envelope:
  ```xml
  <soapenv:Envelope xmlns:soapenv="http://www.w3.org/2003/05/soap-envelope" xmlns:tns="TARGET_NAMESPACE_FROM_WSDL">
    <soapenv:Body>
      <tns:OperationNameResponse>
        <!-- response fields here -->
      </tns:OperationNameResponse>
    </soapenv:Body>
  </soapenv:Envelope>
  ```
- NEVER return a bare XML document without a SOAP Envelope wrapper
- NEVER use JSON as the response body — SOAP responses are always XML
- Response HTTP status code is always 200 for successful SOAP responses

SOAP 1.2 fault format (for error scenarios):
- IMPORTANT: When the description asks for error responses, fault responses, or error scenarios, you MUST generate SOAP fault envelopes — not successful responses with error-like data.
- A SOAP fault response uses HTTP status 500 (not 200) and contains a `soapenv:Fault` element.
- Use `soapenv:Sender` for client-side errors (bad input, not found, validation failures)
- Use `soapenv:Receiver` for server-side errors (internal failures, service unavailable)
- Complete WireMock mapping example for a fault response:
  ```json
  {
    "request": {
      "method": "POST",
      "urlPath": "/namespace/service-path",
      "headers": { "Content-Type": { "contains": "action=\"http://example.com/service/Operation\"" } }
    },
    "response": {
      "status": 500,
      "headers": { "Content-Type": "application/soap+xml; charset=utf-8" },
      "body": "<soapenv:Envelope xmlns:soapenv=\"http://www.w3.org/2003/05/soap-envelope\"><soapenv:Body><soapenv:Fault><soapenv:Code><soapenv:Value>soapenv:Sender</soapenv:Value></soapenv:Code><soapenv:Reason><soapenv:Text>Error description here</soapenv:Text></soapenv:Reason></soapenv:Fault></soapenv:Body></soapenv:Envelope>"
    },
    "persistent": true
  }
  ```
- CRITICAL: The response `"status"` MUST be `500` for fault responses, NOT `200`.

- Return only a JSON array of WireMock mappings.
- Each item MUST be a valid WireMock stub mapping object and MUST include these required top-level fields:
 {
 "request": { ... },
 "response": { ... },
 "persistent": true
 }
- Optional WireMock top-level fields are allowed when needed (e.g., "priority").
- "persistent": true MUST be placed at the top level of the mapping object
- NEVER place "persistent" inside "response" or "request"
- NEVER include unknown fields inside "response" (WireMock will reject them)
- Only use fields defined in the WireMock stub mapping schema below. Any field not in this schema will be rejected.

{{WIREMOCK_SCHEMA}}

- Do not include any explanatory text, only the JSON array.

Generate WireMock JSON mappings for a GraphQL API.

API Specification Summary:
- Title: {{SPEC_TITLE}}
- Version: {{SPEC_VERSION}}
- Operations: {{ENDPOINT_COUNT}}
- Key operations: {{KEY_ENDPOINTS}}

CRITICAL: Use EXACT operation names from the Key operations list above for both the `matchesJsonPath` operationName matcher AND the response `data` key. Do NOT invent or rename operations — use them verbatim.

Namespace:
- API Name: {{API_NAME}}{{CLIENT_SECTION}}

Enhancement Description: {{DESCRIPTION}}

Requirements:

JSON VALIDATION (CRITICAL):
- CRITICAL: Return ONLY valid, parseable JSON. Common pitfalls to avoid: missing commas between array elements, unclosed brackets/braces, trailing commas, unescaped quotes in strings.
- Validate your JSON syntax before returning. Each mapping object must be properly closed and separated by commas.

- IMPORTANT: All GraphQL mocks MUST use POST method and target the /{{NAMESPACE}}/graphql endpoint (GraphQL-over-HTTP pattern).
- CRITICAL: Every mapping MUST include top-level `"persistent": true` to ensure mocks survive Lambda cold starts and are stored in S3.
- Enhance the mappings based on the description (use specific data, behaviors, and naming from the description).
- Include realistic response data that matches both the schema and description.

NON-OVERLAPPING STUB RULES (CRITICAL):
- GraphQL stubs must be NON-OVERLAPPING.
- Never generate more than one stub for the same operation name unless the request matchers are explicitly mutually exclusive.
- If a success case and an error case are both desired for the same operation, they MUST differ by request matcher, such as different variable values or different query content.
- Never generate two stubs that only differ by response while sharing the same request matcher.
- Do NOT generate both a "success stub" and an "error stub" for the same operation with the same matcher — this creates nondeterministic WireMock behavior.
- Never return an empty array.
- If an extra scenario would overlap, omit the extra scenario, not the operation.
- Add error stubs only when explicitly requested and only if the request matcher can differ.

WHICH OPERATIONS TO GENERATE (CRITICAL — read this before generating):
- Generate mocks ONLY for operations explicitly mentioned in the Enhancement Description.
- If the Enhancement Description does not specify particular operations, you MUST generate mocks for EXACTLY the first 5 operations from the Key operations list above. DO NOT generate more than 5 operations by default.
- Count carefully: 1st operation, 2nd operation, 3rd operation, 4th operation, 5th operation. Stop after the 5th operation.
- NEVER generate a mapping for every operation in the Key operations list — this will produce output that is too large.
- One high-quality mapping per selected operation. No more.

GraphQL request matching rules (IMPORTANT):
- GraphQL requests are always POST requests with a JSON body containing `query` or `mutation` fields.
- Use `bodyPatterns` with `matchesJsonPath` on `operationName` as the primary matcher:
  - Example: `{ "matchesJsonPath": "$[?(@.operationName == 'user')]" }` — where 'user' is an exact name from Key operations
  - CRITICAL: Do NOT use the `..` recursive descent operator in JSONPath expressions. Use `$[?(...)]` syntax instead of `$..[?(...)]` syntax.
  - Or match by query content: `{ "matchesJsonPath": "$[?(@.query =~ /.*user.*/)]" }`
  - CRITICAL: For operationName matching, use this exact JSONPath template and do not alter the closing characters: `{ "matchesJsonPath": "$[?(@.operationName == 'ExactOperationName')]" }`
  - The JSONPath string MUST end with `')]` and MUST NOT end with `'))` or omit the final `]`.
- CRITICAL: JSONPath comparison operators MUST have consistent spacing. Use `== 'value'` (with space before ==) not `=='value'` (no space). Example: `$[?(@.operationName == 'pokemon')]`
- You may add extra body matchers on variables or query content only when needed to make scenarios non-overlapping.
- Every mapping MUST include `"urlPath": "/{{NAMESPACE}}/graphql"`.
- Do NOT use `urlPattern`, `urlPathPattern`, `queryParameters`, or path parameters for GraphQL routing.
- Use `bodyPatterns` only to distinguish GraphQL operations on the shared endpoint.

GraphQL response format rules (IMPORTANT):
- ALL GraphQL responses MUST follow the GraphQL response specification:
  - Success: `{ "data": { "<exactOperationName>": { ... } } }` — use the exact name from Key operations
  - Error: `{ "errors": [{ "message": "...", "locations": [...], "path": [...] }] }`
  - Partial: `{ "data": { ... }, "errors": [...] }` (both fields allowed together)
- NEVER return a bare JSON object or array as the response body — always wrap in `data` or `errors`.
- Response HTTP status code is always 200 for GraphQL (even for errors — errors go in the `errors` field).
- Always include `Content-Type: application/json` header in all responses.

Schema-based field validation rules (IMPORTANT):
- Response data MUST include all required fields as defined in the schema for the return type.
- Scalar types must use compatible values (String → string, Int → number, Boolean → boolean, Float → decimal).
- CRITICAL TYPE ENFORCEMENT: Int fields in the schema MUST be JSON numbers (e.g., 25), NEVER quoted strings (e.g., "25"). Boolean fields MUST be JSON true/false, NEVER "true"/"false". Float fields MUST be JSON decimals (e.g., 3.14), NEVER strings. This is the most common validation failure — double-check every scalar value before returning.

EXAMPLE of correct jsonBody with proper scalar types (STUDY THIS):
  Correct:   "jsonBody": {"data":{"pokemon":{"id":25,"name":"pikachu","height":4,"weight":60,"types":[{"slot":1,"type":{"name":"electric"}}]}}}
  WRONG:     "jsonBody": {"data":{"pokemon":{"id":"25","name":"pikachu","height":"4","weight":"60"}}}
  Notice: id (25), height (4), weight (60), slot (1) are bare JSON numbers inside the jsonBody object. name ("pikachu") is a quoted string because it is a String type. Do NOT quote integers — this is the #1 validation failure.
  IMPORTANT: The GraphQL ID type maps to a JSON STRING, not a number. If the schema says `id: ID!`, use "id": "1" (quoted). If the schema says `id: Int!`, use "id": 1 (bare number). Check the schema type carefully for each id field.
- Enum values in responses MUST be one of the valid values defined in the schema.
- List fields must return arrays; object fields must return objects.
- Nested types must include their required fields recursively.
- NESTED REQUIRED FIELDS: When a type has required list fields (e.g., types: [PokemonType!]!, moves: [PokemonMove!]!), you MUST include them with at least one item each. Do NOT omit required list fields — an empty array [] is acceptable but omitting the field entirely will fail validation. Walk through every non-null (!) field in the return type and ensure it exists in your response.
- If the description defines specific entities (e.g., a user with a specific name or ID), those entities MUST appear consistently across ALL relevant operations.

- Return only a JSON array of valid WireMock mappings.
- Each item MUST be a valid WireMock stub mapping object and MUST include these required top-level fields:
 {
 "request": { ... },
 "response": { ... },
 "persistent": true
 }
- Optional WireMock top-level fields are allowed when needed (e.g., "priority").
- "persistent": true MUST be placed at the top level of the mapping object
- NEVER place "persistent" inside "response" or "request"
- NEVER include unknown fields inside "response" (WireMock will reject them)
- Only use fields defined in the WireMock stub mapping schema below. Any field not in this schema will be rejected.

{{WIREMOCK_SCHEMA}}

- Before returning, verify: (1) JSON is valid and parseable, (2) operation count matches requirements (5 by default), (3) JSONPath uses `$[?(...)]` not `$..[?(...)]`, (4) comparison operators have proper spacing
- Do not include any explanatory text, only the JSON array.

# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - Lambda Initialization Failure with Monolithic Module
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: For deterministic bugs, scope the property to the concrete failing case(s) to ensure reproducibility
  - Test that Lambda initialization fails with `NoSuchBeanDefinitionException` for `FunctionCatalog` when deployed from monolithic module
  - Test both runtime and generation Lambda initialization failures
  - The test assertions should verify successful initialization with `FunctionCatalog` bean present (this is the expected behavior after fix)
  - Run test on UNFIXED code (current monolithic module structure)
  - **EXPECTED OUTCOME**: Test FAILS (this is correct - it proves the bug exists)
  - Document counterexamples found: specific error messages, stack traces, CloudWatch log excerpts showing missing `FunctionCatalog` bean
  - Examine Spring context initialization logs to understand root cause
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 2.1, 2.2, 2.3, 2.4_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Runtime Behavior Unchanged After Module Split
  - **IMPORTANT**: Follow observation-first methodology
  - Observe behavior on UNFIXED code for non-initialization operations:
    - Mock serving: runtime Lambda serves mocked endpoints
    - Admin API: create/update/delete mappings, request inspection
    - AI generation: generation Lambda processes AI requests
    - S3 storage: read/write operations for mappings and files
    - WireMock behavior: matching, response serving, extensions
  - Write property-based tests capturing observed behavior patterns from Preservation Requirements
  - Property-based testing generates many test cases for stronger guarantees
  - Test that mock serving produces identical responses across many scenarios
  - Test that admin API operations produce identical results across many scenarios
  - Test that AI generation produces identical outputs across many scenarios
  - Test that S3 operations produce identical results across many scenarios
  - Run tests on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (this confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [x] 3. Split monolithic module into three separate modules

  - [x] 3.1 Create core module structure
    - Create directory `software/infra/aws/core/`
    - Create `software/infra/aws/core/build.gradle.kts` with shared infrastructure dependencies
    - Create `software/infra/aws/core/src/main/kotlin/nl/vintik/mocknest/infra/aws/core/` package structure
    - Create `software/infra/aws/core/src/main/resources/application-core.properties`
    - Configure dependencies: Spring Boot starter (without web), Kotlin AWS SDK (S3), WireMock standalone, domain and application modules
    - Do NOT create Spring Boot application class in core module
    - _Bug_Condition: isBugCondition(input) where input.moduleStructure = 'monolithic' AND NOT functionCatalogBeanExists(input.springContext)_
    - _Expected_Behavior: Each Lambda module has isolated Spring context with FunctionCatalog bean_
    - _Preservation: All runtime behavior unchanged (mock serving, admin API, AI generation, S3 storage, WireMock behavior)_
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.2 Create runtime module structure
    - Create directory `software/infra/aws/runtime/`
    - Create `software/infra/aws/runtime/build.gradle.kts` with Shadow JAR configuration
    - Copy EXACT working Shadow JAR config: mergeServiceFiles(), append META-INF files, minimize blocks
    - Create `software/infra/aws/runtime/src/main/kotlin/nl/vintik/mocknest/infra/aws/runtime/` package structure
    - Create `software/infra/aws/runtime/src/main/resources/application.properties`
    - Configure dependencies: depend on `:software:infra:aws:core`, Spring Cloud Function adapter, AWS Lambda runtime
    - Exclude Bedrock SDK from runtime module dependencies
    - Set Shadow JAR manifest: Main-Class and Start-Class to `nl.vintik.mocknest.infra.aws.runtime.RuntimeApplication`
    - Set Shadow JAR archiveFileName to `mocknest-runtime.jar`
    - _Bug_Condition: isBugCondition(input) where input.lambdaType = 'runtime' AND input.moduleStructure = 'monolithic'_
    - _Expected_Behavior: Runtime Lambda initializes with isolated Spring context containing runtimeRouter function bean_
    - _Preservation: Runtime Lambda serves mocks identically to unfixed code_
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.3 Create generation module structure
    - Create directory `software/infra/aws/generation/`
    - Create `software/infra/aws/generation/build.gradle.kts` with Shadow JAR configuration
    - Copy EXACT working Shadow JAR config: mergeServiceFiles(), append META-INF files, minimize blocks
    - Create `software/infra/aws/generation/src/main/kotlin/nl/vintik/mocknest/infra/aws/generation/` package structure
    - Create `software/infra/aws/generation/src/main/resources/application.properties`
    - Configure dependencies: depend on `:software:infra:aws:core`, Spring Cloud Function adapter, AWS Lambda runtime, Bedrock SDK, Koog framework
    - Set Shadow JAR manifest: Main-Class and Start-Class to `nl.vintik.mocknest.infra.aws.generation.GenerationApplication`
    - Set Shadow JAR archiveFileName to `mocknest-generation.jar`
    - _Bug_Condition: isBugCondition(input) where input.lambdaType = 'generation' AND input.moduleStructure = 'monolithic'_
    - _Expected_Behavior: Generation Lambda initializes with isolated Spring context containing generationRouter function bean_
    - _Preservation: Generation Lambda processes AI requests identically to unfixed code_
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.4 Update settings.gradle.kts
    - Replace `include(":software:infra:aws")` with three new modules:
      - `include(":software:infra:aws:core")`
      - `include(":software:infra:aws:runtime")`
      - `include(":software:infra:aws:generation")`
    - Verify Gradle can resolve all module dependencies
    - Run `./gradlew projects` to confirm new module structure
    - _Bug_Condition: Monolithic module structure causes Spring context ambiguity_
    - _Expected_Behavior: Three separate modules with clear dependency hierarchy (runtime → core, generation → core)_
    - _Preservation: Gradle build continues to work with new module structure_
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.5 Move shared code to core module
    - Move `software/infra/aws/src/main/kotlin/nl/vintik/mocknest/infra/aws/core/storage/` to `software/infra/aws/core/src/main/kotlin/nl/vintik/mocknest/infra/aws/core/storage/`
    - Move `software/infra/aws/src/main/kotlin/nl/vintik/mocknest/infra/aws/core/wiremock/` to `software/infra/aws/core/src/main/kotlin/nl/vintik/mocknest/infra/aws/core/wiremock/` (if exists)
    - Move any other shared infrastructure code from `infra/aws/core/` package to core module
    - Update package imports in moved files if necessary
    - Verify core module compiles: `./gradlew :software:infra:aws:core:build`
    - _Bug_Condition: Shared code in monolithic module contributes to Spring context ambiguity_
    - _Expected_Behavior: Shared code isolated in core module, available to both Lambda modules_
    - _Preservation: S3 storage operations work identically after code move_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 3.1, 3.2, 3.3_

  - [x] 3.6 Move runtime code to runtime module
    - Move `software/infra/aws/src/main/kotlin/nl/vintik/mocknest/infra/aws/runtime/RuntimeApplication.kt` to `software/infra/aws/runtime/src/main/kotlin/nl/vintik/mocknest/infra/aws/runtime/RuntimeApplication.kt`
    - Move `software/infra/aws/src/main/kotlin/nl/vintik/mocknest/infra/aws/runtime/function/` to `software/infra/aws/runtime/src/main/kotlin/nl/vintik/mocknest/infra/aws/runtime/function/`
    - Move `software/infra/aws/src/main/kotlin/nl/vintik/mocknest/infra/aws/runtime/storage/` to `software/infra/aws/runtime/src/main/kotlin/nl/vintik/mocknest/infra/aws/runtime/storage/`
    - Move any other runtime-specific code from `infra/aws/runtime/` package to runtime module
    - Update package imports in moved files if necessary
    - Ensure `RuntimeApplication` has `@SpringBootApplication` annotation with component scanning limited to runtime packages
    - Verify runtime module compiles: `./gradlew :software:infra:aws:runtime:build`
    - _Bug_Condition: RuntimeApplication in monolithic module causes Spring context initialization failure_
    - _Expected_Behavior: RuntimeApplication in dedicated module initializes isolated Spring context with runtimeRouter function bean_
    - _Preservation: Runtime Lambda serves mocks identically after code move_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 3.1, 3.2, 3.4_

  - [x] 3.7 Move generation code to generation module
    - Move `software/infra/aws/src/main/kotlin/nl/vintik/mocknest/infra/aws/generation/GenerationApplication.kt` to `software/infra/aws/generation/src/main/kotlin/nl/vintik/mocknest/infra/aws/generation/GenerationApplication.kt`
    - Move `software/infra/aws/src/main/kotlin/nl/vintik/mocknest/infra/aws/generation/function/` to `software/infra/aws/generation/src/main/kotlin/nl/vintik/mocknest/infra/aws/generation/function/`
    - Move `software/infra/aws/src/main/kotlin/nl/vintik/mocknest/infra/aws/generation/storage/` to `software/infra/aws/generation/src/main/kotlin/nl/vintik/mocknest/infra/aws/generation/storage/`
    - Move `software/infra/aws/src/main/kotlin/nl/vintik/mocknest/infra/aws/generation/ai/` to `software/infra/aws/generation/src/main/kotlin/nl/vintik/mocknest/infra/aws/generation/ai/`
    - Move any other generation-specific code from `infra/aws/generation/` package to generation module
    - Update package imports in moved files if necessary
    - Ensure `GenerationApplication` has `@SpringBootApplication` annotation with component scanning limited to generation packages
    - Verify generation module compiles: `./gradlew :software:infra:aws:generation:build`
    - _Bug_Condition: GenerationApplication in monolithic module causes Spring context initialization failure_
    - _Expected_Behavior: GenerationApplication in dedicated module initializes isolated Spring context with generationRouter function bean_
    - _Preservation: Generation Lambda processes AI requests identically after code move_
    - _Requirements: 2.1, 2.2, 2.3, 2.4, 3.1, 3.2, 3.5_

  - [x] 3.8 Delete old monolithic module
    - Verify all code has been moved to new modules (core, runtime, generation)
    - Delete directory `software/infra/aws/` (old monolithic module)
    - Verify Gradle build works: `./gradlew build`
    - Verify no compilation errors or missing dependencies
    - _Bug_Condition: Monolithic module structure causes Spring context ambiguity_
    - _Expected_Behavior: Monolithic module removed, only new modules remain_
    - _Preservation: All functionality preserved in new module structure_
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.9 Update SAM template with new JAR paths
    - Edit `deployment/aws/sam/template.yaml`
    - Update Runtime Lambda `CodeUri` to point to `../../../software/infra/aws/runtime/build/libs/mocknest-runtime.jar`
    - Update Generation Lambda `CodeUri` to point to `../../../software/infra/aws/generation/build/libs/mocknest-generation.jar`
    - Verify `Handler` property points to `org.springframework.cloud.function.adapter.aws.FunctionInvoker::handleRequest`
    - Verify Runtime Lambda has environment variable `SPRING_CLOUD_FUNCTION_DEFINITION=runtimeRouter`
    - Verify Generation Lambda has environment variable `SPRING_CLOUD_FUNCTION_DEFINITION=generationRouter`
    - _Bug_Condition: SAM template points to old monolithic module JARs_
    - _Expected_Behavior: SAM template points to new module-specific JARs_
    - _Preservation: SAM deployment continues to work with new JAR paths_
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.10 Update build scripts with new Gradle tasks
    - Edit `deployment/aws/sam/build.sh`
    - Replace `./gradlew :software:infra:aws:buildAllLambdas` with `./gradlew :software:infra:aws:runtime:shadowJar :software:infra:aws:generation:shadowJar`
    - Update JAR copy logic to copy from new module locations: `software/infra/aws/runtime/build/libs/` and `software/infra/aws/generation/build/libs/`
    - Test build script: `./deployment/aws/sam/build.sh`
    - Verify both JARs are created in correct locations
    - _Bug_Condition: Build script references old monolithic module tasks_
    - _Expected_Behavior: Build script builds both Lambda modules separately_
    - _Preservation: Build script continues to produce deployable Lambda JARs_
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.11 Update GitHub Actions workflows
    - Edit `.github/workflows/workflow-build.yml`
    - Update build step to run `./gradlew :software:infra:aws:runtime:shadowJar :software:infra:aws:generation:shadowJar`
    - Verify workflow references correct JAR paths for artifact upload
    - Test workflow locally if possible or commit and observe CI run
    - _Bug_Condition: GitHub Actions workflow references old monolithic module tasks_
    - _Expected_Behavior: GitHub Actions workflow builds both Lambda modules separately_
    - _Preservation: CI/CD pipeline continues to work with new module structure_
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.12 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - Lambda Initialization Success with Separate Modules
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - Deploy both Lambda functions from new dedicated modules
    - Invoke runtime Lambda via API Gateway and verify successful initialization
    - Invoke generation Lambda via API Gateway and verify successful initialization
    - Examine CloudWatch logs to confirm `FunctionCatalog` bean is present in Spring context
    - Verify no `NoSuchBeanDefinitionException` errors
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2, 2.3, 2.4_

  - [x] 3.13 Verify preservation tests still pass
    - **Property 2: Preservation** - Runtime Behavior Unchanged After Module Split
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - Verify mock serving produces identical responses
    - Verify admin API operations produce identical results
    - Verify AI generation produces identical outputs
    - Verify S3 operations produce identical results
    - Verify WireMock behavior is unchanged
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)
    - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7_

- [ ] 4. Checkpoint - Ensure all tests pass
  - Run full test suite: `./gradlew test`
  - Run integration tests with SAM local if available
  - Deploy to AWS test environment and verify both Lambdas work correctly
  - Verify runtime Lambda serves mocks successfully
  - Verify generation Lambda processes AI requests successfully
  - Verify no regressions in existing functionality
  - Ensure all tests pass, ask the user if questions arise
